name: new_checks
on:
  workflow_dispatch:
jobs:
  Check_OS:
    name: Check OS
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
#     # 'script' was not transformed because there is no suitable equivalent in GitHub Actions
#     - name: script
#       arguments:
#       - key: scriptBlock
#         value:
#           isLiteral: true
#           value: sh 'cat /etc/os-release | head -2'
  Check_Disk_Space:
    name: Check Disk Space
    runs-on: ubuntu-latest
    needs: Check_OS
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
#     # 'script' was not transformed because there is no suitable equivalent in GitHub Actions
#     - name: script
#       arguments:
#       - key: scriptBlock
#         value:
#           isLiteral: true
#           value: sh 'df -h /var/lib/jenkins/workspace/'
  Check_CPU:
    name: Check CPU
    runs-on: ubuntu-latest
    needs: Check_Disk_Space
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
#     # 'script' was not transformed because there is no suitable equivalent in GitHub Actions
#     - name: script
#       arguments:
#       - key: scriptBlock
#         value:
#           isLiteral: true
#           value: |-
#             sh """
#                                 lscpu | grep -m 3 -E "Architecture|CPU op-mode|CPU\\(s\\)"
#                                 """
  Check_RAM:
    name: Check RAM
    runs-on: ubuntu-latest
    needs: Check_CPU
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
#     # 'script' was not transformed because there is no suitable equivalent in GitHub Actions
#     - name: script
#       arguments:
#       - key: scriptBlock
#         value:
#           isLiteral: true
#           value: sh 'free -h'
